
spring:
  application:
    name: apiGateway

  ## keycloak -resource server
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://127.0.0.1:8443/realms/master
          jwk-set-uri: http://127.0.0.1:8443/realms/master/protocol/openid-connect/certs


  config:
    import: optional:configserver:http://localhost:8888
